{
  "name": "shadow-collector-api",
  "version": "1.0.0",
  "packageManager": "bun@1.0.0",
  "description": "API service for receiving label data and storing to MinIO",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "test:e2e": "bun test tests/e2e",
    "migrate": "bun run src/scripts/migrate-storage.ts"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@hono/zod-validator": "^0.4.0",
    "@aws-sdk/client-s3": "^3.500.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0",
    "typescript": "^5.3.0"
  }
}
